# SDD ledger — plan: docs/superpowers/plans/2026-09-05-m2b-reporting-and-adjudication.md

Spec: docs/superpowers/specs/2026-08-31-paragon-platform-design.md (read; binding authority)
Branch: feat/m2b-reporting (from main @ 6cde170)
Baseline before Task 1: npm run check green — 83 files, 1209 tests, exit 0.

## Preflight conflict scan

### Task pairs sharing a file or an interface

| Pair | Produced vs consumed | Finding |
|---|---|---|
| T1 ↔ T2 | `is_valid_scoreline(smallint,text[])`; both append to `reports.test.ts` | Clean. T2's tests reuse `makeMatch` from T1's block; execution order holds it. |
| T1 ↔ T3 | `reports.test.ts` append | Clean. |
| T2 ↔ T3 | `submit` helper defined in T2, used by T3 | Clean, order-dependent; ordering holds. |
| T2 ↔ T4 | `submit_report(uuid,text[])` vs rpc args `p_match_id`/`p_wins` | Clean, names match. |
| T1 ↔ T4 | `matches.state` 7 values vs `MatchState` union | Clean, all 7 agree. |
| T4 ↔ T5 | `Match`, `Side`, `submitReport`, `toMatchTerms`, `toMyTerms` | Clean. |
| T4 ↔ T5 | `matchmaking.ts` re-export vs `MatchmakingScreen` import | Clean — screen imports stay valid. |
| T3 ↔ T6 | `sweep_matches()` vs roundtrip check 6 | Clean. |
| T4 ↔ T6 | `matches.ts` consumed by the roundtrip | Clean. |

### Each task against itself

| Task | Finding |
|---|---|
| T1 | Agrees. Test imports `POLICY_DENIED` without using it — see Ruling 3. |
| T2 | Agrees. |
| T3 | Agrees. Also edits HANDOFF.md, which is documentation, not gated. |
| T4 | Agrees. |
| T5 | **DEFECT** — test imports `{ render } from '../../../test/render'`. Neither exists: the helper is `app/src/test/render.tsx`, exporting **`renderApp`**, reached from `src/screens/__tests__/` as `'../../test/render'`. See Ruling 2. |
| T6 | Specified in prose, not full code. See Ruling 4. |

## Rulings

Ruling 1: Feature branch `feat/m2b-reporting` in the existing checkout rather than a git worktree — the user's running dev server, their two signed-in browser origins, and the 12-container local Supabase stack are all bound to this checkout, and a worktree would need a duplicate node_modules and would contend for the same container names and ports. Cost if wrong: none to the code; create a worktree later and the branch moves with it.

Ruling 2: Task 5 uses `renderApp` from `'../../test/render'`, not `render` from `'../../../test/render'`. The plan's import is wrong on both the name and the depth. Cost if wrong: none — `renderApp` is the only export the file has.

Ruling 3: T1's unused `POLICY_DENIED` import is left to the implementer's discretion. oxlint runs with cwd `app/`, and `supabase/tests/` sits outside it, so an unused import there is not a gate failure. Cost if wrong: one lint error at T1's gate, one import removed.

Ruling 4: Task 6 is accepted as prose. It is a fixture, its seven checks are individually enumerated, and `app/tools/m2a-roundtrip.ts` is the pattern to copy. Cost if wrong: one clarification round with the implementer.

## Progress

Task 1: complete (commit b379c29, review pending) — see Ruling 5/6 first.

Ruling 5: `npm run check:db` was ALREADY RED on main before this plan started. Measured, not assumed: checked out baseline 6cde170, ran `db:reset` + `check:db`, got the byte-identical 8 failures (139 tests, 8 failed). The implementer's "pre-existing" claim is confirmed. The plan's Global Constraint "check:db green at the end of every task" is therefore unsatisfiable as written; for this plan it is read as "no NEW failures, and the pre-existing 8 fixed by Task 1b". Cost if wrong: none — the measurement is reproducible with two commands.

Ruling 6: Insert an unplanned Task 1b to fix the 8 pre-existing failures BEFORE continuing to Task 2, rather than parking them. Two groups, both regressions from commit 996be91 (`20260904190000_friend_codes_are_twelve_digits.sql`), which is DEPLOYED TO PRODUCTION:
  (a) CRITICAL — that migration rewrote `handle_confirmed_user()` and dropped the `display_name/go_username/birth_date is null -> return new` guard added by `20260901225208`. Only the `email_confirmed_at` check survives. This is verbatim the defect HANDOFF.md documents as "OAuth signup was impossible at the database level": Discord is the only provider button, GoTrue inserts a provider account already confirmed with none of those three fields, the INSERT raises not_null_violation, which is NOT the unique_violation the handler forgives, so it propagates and takes the whole auth.users INSERT with it. 5 failing tests have been reporting this since 2026-09-04.
  (b) MINOR — 3 rls.test.ts fixtures write friend codes that predate the `friend_codes_twelve_digits` constraint the same migration added, so they now violate it. Stale tests, not a product bug.
  Rationale for fixing rather than parking: a red gate makes every later task's gate unreadable ("did I break it, or was it already broken?"), which degrades every review left in this plan; and M2b will be deployed on top of (a).
  Cost if wrong: one extra migration and a test-fixture edit, both revertable, on a branch that is not yet merged.
Task 1+1b: review clean on spec (✅), 1 Important + 3 Minor on quality. Entering fix round 1.
Task 1: minor (deferred): `is_valid_scoreline`'s `other-side < needed` clause is mathematically redundant given the other two conditions. Harmless, and it documents the rule; left alone.
Task 1: minor (deferred): neither `match_reports.reporter_id` nor `match_rounds.winner` is constrained to be one of the match's two players. Not exploitable while all client writes are revoked and no INSERT policy exists. CARRY INTO TASK 2 — `submit_report` becomes the only writer and is where that invariant gets enforced.

Ruling 7: the deny-test gap on the friend-code policy (no test proves the code is HIDDEN once `confirmed`/`unverified`) is promoted from Minor into fix round 1. The plan's Global Constraints say "Every policy gets an allow test AND a deny test" without exception, so this is spec compliance, not polish. Cost if wrong: two extra assertions in a test that already exists.

Ruling 8: fix round 1 EDITS `20260905120000_match_reports_and_rounds.sql` in place rather than adding a follow-up migration. The append-only rule binds DEPLOYED migrations; this one exists only on the unmerged branch `feat/m2b-reporting` and has never reached production. A corrective migration here would ship a permanent two-step apology for a file nobody outside this branch has run. Cost if wrong: if the branch had somehow been deployed, the edit would not re-run — checked, it has not; `main` is at 6cde170.
Task 1: fix round 1/5 (2 addressed, 0 open — anon write grants revoked on both tables; deny tests added for confirmed/unverified friend-code visibility and for anonymous writes; commits 85998a0..36dcb05). Verified by the controller: check:db EXIT=0 145/145, check EXIT=0 1209/1209, and information_schema.role_table_grants returns EMPTY for anon+authenticated INSERT/UPDATE/DELETE on match_reports and match_rounds.

Ruling 9: the superpowers `sdd-workspace` / `review-package` scripts rewrite `.superpowers/sdd/.gitignore` to a bare `*` on every run, silently reversing commit 26bb6a3, which deliberately made the decision ledgers tracked (HANDOFF.md documents this and tells the next session to read them). Restored after each script run and this plan's ledger committed explicitly. Cost if wrong: none — worst case the ledgers are tracked when somebody wanted them ignored, which is the state the repo already chose.
Task 1: minor (deferred): the friend-code policy's excluded-state coverage omits `abandoned` — neither the allow nor the new deny test exercises it.
Task 1: complete (commits 6cde170..36dcb05, review clean after 1 fix round). Includes unplanned Task 1b (43189ea, 85998a0) restoring the OAuth signup guard.
Ruling 8 reconfirmed at completion: `main` is still at 6cde170 and this branch is unmerged, so the in-place migration edit never reached a deployed environment.
Task 2: review spec ✅, 1 Important (row lock untested and structurally untestable on the shared max:1 client) + 1 Minor. Entering fix round 1.

Ruling 10: the reviewer's ⚠️ "cannot verify whether amend_deadline expiry is enforced elsewhere" is resolved by me, not by a fix: Task 3 (`sweep_matches`) is exactly that enforcement — `mismatch` past `amend_deadline` becomes `disputed`. `submit_report` deliberately does not check the clock, because a function that both accepts amends and expires them would race its own sweep. Not a gap. Cost if wrong: Task 3's tests fail to find the transition and the gap surfaces one task later.

Ruling 11: the Important finding enters the fix loop rather than being parked, because the repo already answers it. `supabase/tests/pairing.test.ts:25-37` documents the identical `max: 1` limitation verbatim and supplies the pattern — `const conn = () => postgres(CONNECTION_STRING, { max: 1 })` for independent connections, plus a helper that holds a row lock across an await boundary. A guarantee the plan calls "the whole reason the common path is safe" should not be the one thing in this task with no test, when the pattern to test it already exists twelve files away. Cost if wrong: one extra test and two connections; if the pattern does not transfer, park it and the manual proof in the review still stands.

Task 2: minor (deferred, folded into fix round 1 as a comment correction only): the `delete from match_rounds` before re-adjudication is unreachable today — the confirm branch is a one-time terminal transition, so `match_rounds` is never non-empty when it runs. The delete is harmless and correct if a later task ever reopens a match; its comment claims a path the state guard does not permit.

## Out-of-band: OAuth fix shipped to production 2026-09-05 14:37Z
At the user's instruction, cherry-picked 43189ea + 85998a0 onto main as bb847fe + bc31250 and pushed.
Verified BEFORE pushing: main alone is green — check:db 139/139 EXIT=0 (was 8 failing), check 1209/1209, guard present in a DB built from main's migrations only, and match_reports/match_rounds correctly ABSENT (count 0), proving the fix is self-contained.
Verified AFTER pushing, by measurement rather than by the check run: `supabase migration list --linked` shows 20260905123000 with remote == local; `supabase db dump --linked` carries the three-way null guard at lines 255-260 of production's own schema, correctly placed after the email_confirmed_at check and before the profiles INSERT.
Known consequence: 20260905120000 and 20260905121000 stay on this branch with timestamps EARLIER than the now-applied 20260905123000. Supabase applies any migration whose version is unrecorded, so they will apply out of order when M2b merges. They are independent of the guard fix (new tables vs a function replace), so this is safe — but it is a real ordering wrinkle worth knowing at merge time.
Task 2: fix round 1/5 (2 addressed, 0 open — row-lock race test on two independent connections, proven to FAIL without `for update` with ['reported','reported']; stale comment corrected, SQL byte-identical; commits 04f280c..146c1b8).
Task 2: complete (commits 6915268..146c1b8, review clean after 1 fix round). check:db 150/150, check 1209/1209, both verified by the controller.
Task 2: minor (deferred): the new race test closes its two connections after the race but outside a try/finally, so an unexpected throw inside the race would leak sockets. Mirrors the accepted precedent at pairing.test.ts:232-238; not introduced here.

Ruling 12: Task 3's implementer found that the brief's `sweep_matches` SQL grants `service_role` without first revoking the EXECUTE that Postgres auto-grants to PUBLIC on function creation — so the brief as written violated its own "service_role ONLY" constraint. They added `revoke all on function ... from public, anon, authenticated` before the grant, matching five existing precedents (20260903005933:165-166, 20260904071717:97, 20260904174517:137). Accepted: this is a plan defect, the fix is right, and the precedent is unambiguous.

Ruling 13: the same defect exists in TASK 2, which I already marked complete. Measured directly from pg_proc.proacl: `submit_report` carries `=X/postgres` (PUBLIC) and `anon=X`, while every other definer function in the schema has PUBLIC revoked. Not a live exploit — the function's own `me is null or me not in (m.player_a, m.player_b)` guard refuses every anonymous caller before any write — but it leaves an anonymous caller able to take a `for update` row lock on a `matches` row and to distinguish 'no such match' from 'this match is not yours', which is a match-id existence oracle. Both of Task 2's reviewers and I missed it. Folding the one-line revoke into Task 3's fix round rather than reopening Task 2, since it is the same class of finding in an adjacent file and one round fixes both. Cost if wrong: a revoke that was already implied by RLS, reverted in one line.
Task 3: review spec ✅, quality approved for its own deliverable. 1 Important (submit_report PUBLIC/anon grant, a Task 2 leftover) + minors. Fix round 1 dispatched and verified: ACLs now `submit_report | postgres | authenticated | service_role` and `sweep_matches | postgres | service_role` — no PUBLIC, no anon. check:db 155/155, check 1209/1209.
Task 3: minor (deferred): `sweep_matches`'s `amend_deadline is not null` guard is redundant today — `submit_report` is the only writer of state='mismatch' and always sets the deadline in the same statement. If a future writer sets 'mismatch' without a deadline, that row is unreachable by BOTH updates and becomes permanently unresolvable.
Task 3: minor (deferred): the coordinator maps an RPC error to 0 (`sweptMatches ?? 0`), indistinguishable from "nothing to sweep". Pre-existing pattern — `paired` and `swept` do the same — and HANDOFF.md already directs operators to net._http_response rather than the JSON body.
Task 3: minor (deferred): `disputed`/`unverified`/`abandoned` exclusion from the sweep is logically implied but untested; only `confirmed` has an explicit "left alone forever" test.
Task 3: minor (deferred, informational): an inherent boundary race — a genuinely differing second report landing in the same instant a sweep crosses the 48h mark will block on `for update`, then see state `unverified` and be refused as "no longer accepting reports". Inherent to any age-cutoff design, not a defect in this SQL.

Ruling 14: dispatched Task 4 concurrently with Task 3's scoped re-review. The skill forbids parallel IMPLEMENTATION subagents because they conflict; these two do not overlap at all — the re-reviewer only reads SQL migrations and `supabase/tests/`, while Task 4 writes only `app/src/lib/`. Task 4 consumes just the `submit_report` RPC contract, which is settled and green. Task 4 is also instructed not to run `db:reset` or `check:db`, so it cannot disturb the database the re-review reasons about. Cost if wrong: if the re-review reopens Task 3, Task 4's commit sits on top of a reopened commit — recoverable by fixing forward, since the two touch disjoint files.
Task 3: fix round 1/5 (2 addressed, 0 open — submit_report PUBLIC/anon revoked in place with a test that asserts `permission denied for function submit_report` locally rather than widening the shared regex, confirmed it would fail if the revoke were removed; `reported`-state sweep covered via the real submit_report path; commits 43a1a70..a1b90aa).
Task 3: complete (commits 146c1b8..a1b90aa, review clean after 1 fix round). check:db 155/155, check 1209/1209.
Task 4: review spec ✅. Pre-existing test edits audited line by line and cleared — all fixture-widening for four newly required `Match` fields; every `toEqual` stayed a `toEqual`, no `toMatchObject` anywhere in the diff, no assertion dropped. 1 Important (unguarded `mySide` when the session id is undefined) + 2 Minor (comments made stale by the code move). Entering fix round 1.
Task 4: fix round 1/5 (3 addressed, 0 open — myMatches returns [] with no session rather than fabricating a seat, proven to FAIL without the guard; two stale comments repointed; commits 6c6797f..06fc032). check 1213/1213.
Task 4: minor (deferred, PRE-EXISTING, not from this plan): `CoresScreen` in `app/src/screens/__tests__/screen-leaves.test.tsx` timed out once on a full-gate run and passed on immediate re-run. A flake in a suite this plan never touched. Worth a look if it recurs — a gate that fails at random is a gate people learn to re-run instead of read.

Ruling 15: the repeated `npm run db:reset` runs this plan required have WIPED the local fixture accounts — `test-opponent-{1,2}@example.test`, `friendcode-proof@example.test`, the six seeded offers and both bots' rosters are all gone. Measured: auth.users holds 39 rows, every one of them a UUID-named fixture left behind by the db suites, none of them a seeded bot. Consequences: (a) Task 6's roundtrip must CREATE its own accounts through the real signup-and-mailpit path rather than assuming the bots exist — it cannot just sign in; (b) the two-origin browser setup demonstrated earlier in the session is broken until `tools/opponents.ts` is re-run. Cost if wrong: none, this is an observation about local state, not a code decision.

Deferred (pre-existing, whole-repo): the database suites create rows in `auth.users` and clean only their own domain tables, so orphan fixture accounts accumulate across runs — 39 of them right now. Harmless locally, but it means `select count(*) from profiles` is not a meaningful assertion in any suite, and a future test that scans all profiles will get slower every run.
Task 4: complete (commits a1b90aa..06fc032, review clean after 1 fix round). check 1213/1213, check:db 155/155 unaffected.
Task 4: minor (deferred): three no-session behaviours now coexist — `myMatches` returns [], `myReport` returns null, `myOffers` throws. Each is defensible alone; together they are three answers to one question. Worth a consistency pass, not a defect.

Ruling 16: the plan's Task 5 test imported `@testing-library/user-event`, which is NOT a dependency of this project and which no other test uses — a third defect in my own plan text, after the `render`/`renderApp` import (Ruling 2) and the `refusal()` calling convention (Task 1). The implementer rewrote the three tests with `fireEvent`, the house convention used by `interactions.test.tsx`, keeping the assertions identical, and left a comment saying why. Accepted. Cost if wrong: none — adding a devDependency to satisfy a plan I wrote badly would have been the worse trade.

Ruling 17: accepted Task 5's expansion beyond its stated file list into `app/src/state/AppState.tsx` and `app/src/App.tsx`. The brief said to register `match` in `screens.ts`, and in this codebase that is not a self-contained act: `App.tsx`'s Nav renders one live tab per `SCREEN_DEFS` entry unconditionally, and `Screen` is a union in `AppState.tsx`, so registering without wiring ships a nav tab that renders nothing. The plan's file list was incomplete, not the implementer's scope discipline. Cost if wrong: `activeMatch` is new shared state on a context every screen consumes; if it proves wrong it is a one-line revert plus the `case 'match'` in App.tsx, and the implementer recorded that revert path in the report.
Task 5: review spec ✅, quality NOT approved — 2 Important. (i) `setRound` backfills skipped rounds to `false` and visibly marks "they won" for rounds the player never clicked, so "unanswered" and "they won" are one state; combined with truncation-on-edit a player can submit a scoreline misrepresenting the match. Blast radius limited by `submit_report`'s element-wise array comparison, which turns it into a spurious mismatch rather than a corrupt `match_rounds`. (ii) the load effect's deps are `[match.id, match.mySide]` and `App.tsx` renders MatchScreen with a static key, so an in-session transition to `confirmed` never refetches — the adjudicated list stays empty and the form vanishes, leaving a headline and nothing else; `activeMatch.state` also goes stale across nav round-trips. Entering fix round 1.
Task 5: minor (deferred): `isCompleteScoreline`'s `needed()` does not itself require `bestOf in (3,5)` the way the SQL does. Unreachable today; would matter if `matches.rounds` ever carried another value.
Task 5: fix round 1/5 dispatched — three-state rounds (boolean|null) so an unanswered round is neither pressed nor submittable; `match.state` added to the effect deps plus self-reconciliation via `myMatches()` so a transition refreshes on the same static-keyed instance; "Amend my report" label tested; the misattributed comment repointed at submit_report's real guard. check 1220/1220. Commit 464997e.

Ruling 18: accepted the implementer overriding my instructed test sequence for Finding 1. I specified "click round 3, then correct with round 1"; they proved that sequence cannot discriminate the bug — editing index 0 truncates the whole array under both the old and new `setRound`, so both produce identical state — and substituted "round 3, then round 2", which under the old code leaves a submittable and dishonest `[false, false]`. Their revert experiment then showed both new tests failing with `aria-pressed="true"` where `"false"` was expected. Overriding a controller instruction with a demonstrated reason is the behaviour I want; the re-reviewer is checking the reasoning independently. Cost if wrong: a test that pins less than I asked for, which the scoped re-review is explicitly tasked to catch.
Task 5: fix round 1/5 (4 addressed, 0 open; commits a106bf5..464997e). Re-review independently verified: the three-state model has no null->false coercion anywhere; the substituted test sequence is the ONLY one of the two capable of falsifying the fix, so Ruling 18 stands; the refresh mechanism cannot oscillate (my termination premise was wrong — the effect never calls onChanged, data flow is strictly one-directional); the repointed comment is TRUE against submit_report's real guard.
Task 5: complete (commits 06fc032..464997e, review clean after 1 fix round). check 1220/1220.
Task 5: minor (deferred): after each submit the screen fires a redundant second `myMatches()` on top of App.tsx's own onChanged fetch — a wasted round trip, not a defect.
Task 5: minor (deferred): `match-screen.test.tsx` uses a match id as a round `winner` (a player-id field) to exercise the "you won" branch; works by coincidence, sloppy fixture data.
Task 5: minor (deferred): HEADLINE's `unverified`/`abandoned` copy was inferred rather than checked against M2a's own wording.
Task 6: complete (commit 5e677ae, no fix round needed). The controller RE-RAN the roundtrip independently: 11 passed, 0 failed, exit 0. Check 2 and check 4 — the sealing rule through PostgREST — both show bot2 getting exactly 0 rows, not an error. Check 5 shows match_rounds winners in round order with the right profile ids. Check 7 raises verbatim "this match is no longer accepting reports". The script also correctly exits 2 with a clear message when SUPABASE_SERVICE_ROLE_KEY is absent, and cleans up after itself (auth.users back to the baseline 39 rows).
Task 6: minor (deferred): the script does not update HANDOFF.md's coordinator response shape — correctly, since it never calls the coordinator. That edit belonged to Task 3 and was done there.

## Final whole-branch review — 1 Critical, 4 Important, 7 Minor

Ruling 19: C1 is real and I verified all four legs myself. `confirm_offer` (20260903011151:37-42) inserts the match at CONFIRMATION with created_at = now(); `match_offers.scheduled_for` is constrained only `> created_at`, never bounded above; `matches` has no play-time column (checked information_schema: id, player_a, player_b, format_version_id, rules_hash, team_a, team_b, data_rev, seed, rounds, state, source, created_at, rating_counted, amend_deadline); and the UI's datetime-local input has no `max`. So a match agreed Monday to be played Friday is swept to `unverified` on Wednesday — before it is played — after which reporting is refused, the opponent's friend code disappears, and the screen tells them they failed to report in time. No task-scoped review could see this: `scheduled_for` lives on `match_offers` in an M2a migration that Task 3's reviewer was never shown. FIXING, not deferring.

Ruling 20: fixing C1 by putting the play time on `matches` (`play_after timestamptz`, null for live/queue matches, set from `o.scheduled_for` by the offer path) and sweeping on `coalesce(play_after, created_at)`. Chosen over joining back to `match_offers` because the sweep should not have to know offers exist, and over excluding `source='offer'` because that defers the bug rather than fixing it. Cost if wrong: one nullable column and one coalesce, both revertable.

Ruling 21: I1 fixed as `rating_counted = (m.source = 'queue')` rather than the reviewer's suggested constant `false`. The spec confines rating to the open queue on three canonical leagues; `source` is the half of that predicate the row can actually answer, and `matches` carries no `league` column. `true` overclaims, `false` makes the column meaningless and needs rework anyway; `source = 'queue'` is exactly as much as is derivable today. Comment must name that league eligibility is NOT derivable and M4 has to re-derive rather than trust this. Cost if wrong: M4 re-derives, which it should anyway.

Ruling 22: promoting I3 (coordinator swallows the sweep_matches RPC error, so a dead sweep returns 200 with matches:0 and the documented health check reports health). The ledger deferred it citing the paired/swept precedent; the reviewer is right that the precedent does not transfer — those two have been observed working in production, sweep_matches never has, it is the only thing that moves a match out of `mismatch`, and its silent failure looks exactly like "no disputes".

Ruling 23: renaming 20260905120000/121000/122000 to 124000/124100/124200 so they sort strictly AFTER the already-deployed 20260905123000. The out-of-order apply is provably safe (I read all four; no cross-dependency in either direction, both orderings yield an identical schema) but the rename is free while they are unmerged and deletes the whole question, including "what does the Supabase GitHub integration do with an out-of-order version", which is version-dependent and which I cannot verify.

Ruling 24: I2 (the first tick after deploy sweeps every pre-existing `paired` match older than 48h) is NOT a code fix — it needs a production measurement the user must run. Mitigation is free and already structural: merging deploys migrations automatically, but the coordinator Edge Function is a MANUAL deploy step, so sweep_matches exists but nothing calls it until that step. Sequence is merge -> measure -> triage -> then deploy the coordinator. Surfacing to the user rather than guessing.

Ruling 25: I4 (`disputed` is a sink that retains friend-code visibility forever) deferred to M2c, which owns the evidence ladder and the `unverified` exit. Recorded as a known consequence rather than left to be discovered. Cost if wrong: a disputed match's opponent keeps a friend code longer than the spec's "currently active match" wording intends, on a state nothing can currently produce in production.

## Final fix wave: all four remaining findings closed

C1 (Ruling 19/20) fixed: `matches.play_after timestamptz` added in `20260905124000_match_reports_and_rounds.sql` (unmerged, edited in place); `confirm_offer()` (20260903011151, deployed) replaced via new migration `20260905124300_scheduled_matches_carry_their_play_time.sql` to set it from `o.scheduled_for`; `accept_offer()`'s live-offer insert needs no change since `scheduled_for` is null on that branch by construction, so `play_after` is null there exactly as intended. `sweep_matches()` now ages on `coalesce(play_after, created_at)`. Pinned with three new tests in `reports.test.ts`: a scheduled match 96h from play / 49h past confirmation is NOT swept; one whose play time passed 48h+ ago IS swept; a queue match with no `play_after` still sweeps on `created_at` as before. Revert experiment performed exactly as instructed: reverted the predicate to bare `created_at`, `db:reset`, re-ran — the first two new tests failed (one wrongly swept, one wrongly not swept), the queue test still passed; restored the fix, `db:reset`, re-ran — all 3 pass. This is the strongest form of evidence this ledger has for any fix in the branch.

I1 (Ruling 21) fixed: `rating_counted = (m.source = 'queue')` in `submit_report`'s confirm branch, with a comment stating this is only half the spec's predicate (no `league` column exists yet) and that M4 must re-derive eligibility rather than trust the flag. Test added: a confirmed `queue` match gets `rating_counted = true`, a confirmed `offer` match gets `false`. `makeMatch` in `reports.test.ts` now takes an optional `source` parameter (default `'queue'`, preserving every existing call site).

I3 (Ruling 22) fixed: `supabase/functions/coordinator/index.ts` now destructures `error` from the `sweep_matches` RPC call and returns `500` with the error's message when it fails, rather than folding it into `matches: sweptMatches ?? 0`. `paired`/`swept` deliberately left swallowing errors, per Ruling 22's own scoping. `docs/superpowers/HANDOFF.md` updated at both places that describe the healthy tick's body (the "prove the coordinator ticks" section and the "confirm" section) to note a `500` means `sweep_matches` itself failed. `verify:coordinator-bundle` (part of `npm run check`) confirms the edited coordinator still builds against the same rules bundle.

I5/minor (Ruling per FIX 4 brief) fixed: `sweep_matches`'s mismatch->disputed UPDATE now reads `and (amend_deadline is null or amend_deadline <= now())`, closing the theoretical stuck-row class where a future writer could set `state='mismatch'` without a deadline. Comment states this is unreachable today (submit_report is the only writer and always sets the deadline) but free to close.

Verification, in order: `db:reset` EXIT=0 (migrations apply 20260905124000..300 in strict numeric order, no OAuth-guard conflict); `check:db` EXIT=0, 159/159 (was 155, +4: three FIX-1 tests, one FIX-2 test); `check` EXIT=0, 1220/1220 (unchanged — no app-level test touched this wave; `verify:coordinator-bundle` passed, confirming the coordinator edit still builds); two-account roundtrip (`tools/m2b-roundtrip.ts`) 11/11 passed, exit 0, unchanged from Task 6's baseline. Full re-verification repeated after the FIX-1 revert experiment restored the fix: identical results.

No disagreements with the brief. `accept_offer`'s "live path — check both" instruction resolved to "no code change needed there": its `scheduled_for is null` branch means `play_after` is null on that insert by construction, which is exactly the intended behaviour for a live offer, so it was left as `create or replace`-free (only `confirm_offer` needed the new migration).

## Final fix wave: complete
All of C1, I1, I3 and T3 verdicted ADDRESSED by the scoped re-review; no new breakage. Verified by the controller: check:db 159/159 EXIT=0, check 1220/1220 EXIT=0, roundtrip 11/11 EXIT=0, `matches.play_after` present in the live DB, and every one of the four fixes confirmed in source. The re-review additionally diffed the replaced `confirm_offer` statement by statement against the deployed original and confirmed no guard was dropped, and confirmed `accept_offer`'s live path cannot reach an INSERT with a non-null scheduled_for.
PLAN COMPLETE — all 6 tasks plus the unplanned Task 1b, 15+4 commits, 6cde170..HEAD.
